<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="utf-8"/>
    <meta http-equiv="X-UA-Compatible" content="IE=edge"/>
    <meta name="viewport" content="width=device-width, initial-scale=1.0"/>
    <title>Hyperlog · Phoenix Framework</title>
    <link rel="stylesheet" href="<%= Routes.static_path(@conn, "/css/app.css") %>"/>
    <script src="https://kit.fontawesome.com/a0b4631295.js" crossorigin="anonymous"></script>
  </head>
  <body>
    <header>
      <a href="/" class="logo"><img class="logo" src="<%= Routes.static_path(@conn, "/images/hyperlog.png") %>" alt="Logo"/></a>
      <%= if @conn.assigns.current_user do %>
        <nav>
          <ul class="nav__links">
            <li><a href="<%= "/u/#{@conn.assigns.current_user.username}" %>">Profile</li>
            <li><a href="/courses">Courses</a></li>
            <li>
              <%= form_for @conn, Routes.auth_path(@conn, :delete), fn _f -> %>
                <%= submit "Logout", class: "logout-button" %>
              <% end %>
            </li>
          </ul>
        </nav>
      <% else %>
        <nav>
          <ul class="nav__links">
            <li><a href="#">How?</a></li>
            <li><a href="#">About</a></li>
          </ul>
        </nav>
        <a class="cta" href="/auth/github"><button class="github-button"><i class="fab fa-github-square fa-lg"></i> Sign in with Github</button></a>
      <% end %>
    </header>
    <main role="main" class="container">
      <%= if get_flash(@conn, :info) do %>
        <p class="info-msg" role="alert"><%= get_flash(@conn, :info) %></p>
      <% end %>
      <%= if get_flash(@conn, :error) do %>
        <p class="error-msg" role="alert"><%= get_flash(@conn, :error) %></p>
      <% end %>
      <%= render @view_module, @view_template, assigns %>
    </main>
    <script type="text/javascript" src="<%= Routes.static_path(@conn, "/js/app.js") %>"></script>
    <script type="text/javascript" src="https://cdnjs.cloudflare.com/ajax/libs/prism/1.17.1/components/prism-core.min.js"></script>
    <script type="text/javascript" src="https://cdnjs.cloudflare.com/ajax/libs/prism/1.17.1/plugins/autoloader/prism-autoloader.min.js"></script>
  </body>
</html>
